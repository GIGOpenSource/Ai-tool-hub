# Bug 与修复日志

本文档汇总**已在代码或流程中落实的修复/改进**，以及**文档中登记的已知问题**（含风险与待办）。**Git** 在撰写时无独立「fix」类提交可引用；条目主要来自 [03-开放事项总表.md](./03-开放事项总表.md)、[04-P0安全与联调备忘.md](./04-P0安全与联调备忘.md)、[05-工程优化与运维备忘.md](./05-工程优化与运维备忘.md)、[14-AI-SEO与数据打通评估-项目现存问题.md](./14-AI-SEO与数据打通评估-项目现存问题.md)。关闭条目时请在本表标注日期并回写对应需求/开放事项文档。

---

## 1. 已落实的修复与改进（工程行为）

| ID / 主题 | 问题或目标 | 修复或改进摘要 | 参考 |
|-----------|------------|----------------|------|
| **生产 JWT 弱密钥** | 生产使用默认/过短/占位 **`JWT_SECRET`** 导致高风险 | **`ENVIRONMENT=production`** 时 **`enforce_production_secrets()`** 在 lifespan **最先**执行，弱/占位则**拒绝启动** | [03](./03-开放事项总表.md) §0.1、`backend/app/env_guard.py` |
| **生产覆盖管理员口令** | 每次启动把已存在管理员密码重置为演示值 | **`ensure_dev_accounts`**：生产对已存在用户**不覆盖** **`password_hash`**（首启插入后仍须立即改密） | [03](./03-开放事项总表.md) §0.1、[04](./04-P0安全与联调备忘.md) |
| **发布回归无自动化抓手** | 人工走查成本高、易漏基础接口 | **`backend/scripts/publish_smoke.sh`** 抽样关键路径；**CI** 起 API 后执行（**不能替代**完整 TC-EXEC） | [03](./03-开放事项总表.md) §0.2、`.github/workflows/ci.yml` |
| **CORS 局域网联调** | 开发机用局域网 IP 访问前后端跨域失败 | **`ALLOWED_ORIGINS` 未设置**时启用 **`allow_origin_regex`** 匹配本机与常见私网段 | [17](./17-程序说明书（完整）.md) §4.3、`main.py` **`_cors_allow`** |
| **启动钩子过时** | `on_event` 等旧生命周期不利于异步收尾 | 使用 **`lifespan`**；停机 **cancel** 爬虫调度并 **`await`** 收尾 | [17](./17-程序说明书（完整）.md) §4.2 |
| **前端体积与死代码** | 未引用 UI 组件与依赖膨胀 | **BACKLOG-A**：删除零入边组件、精简 **npm** 依赖；**`npm run build`** 验收 | [05](./05-工程优化与运维备忘.md) |
| **`site_json` 迁移覆盖** | 种子升级误覆盖运营已改 JSON | **`migrate`** 对缺键**合并**、**不覆盖**已有键（**`dashboard` / `home_seo`** 等运维约定） | [05](./05-工程优化与运维备忘.md) |
| **P-AI-01** | **`crawler_snapshot`** 与内容爬虫语义混淆 | 新增推荐占位符 **`{{seo_indexing_snapshot}}`**（sitemap/robots）；**`{{crawler_snapshot}}`** 仍为**同内容别名**；默认种子与后台示例模板已改用新名 | `ai_insight_service.py`、`ai_insight_prompt_defaults.py` |
| **P-AI-02** | 出站官网点击无独立埋点 | **`POST /api/track/outbound`** + 表 **`outbound_click_log`**；详情页点击上报；**`traffic_snapshot.outbound_official_clicks_7d`** 注入 AI 分析 | `track.py`、`analytics_service.py`、`ToolDetailPage.tsx` |
| **P-DOC-01** | **`build_snapshots`** 无自动化验收 | 脚本 **`backend/scripts/ai_insight_snapshots_acceptance.py`**；CI **`backend-py-compile`** 中执行 | `.github/workflows/ci.yml` |
| **P-DOC-02** | 需求 12 §7 与发布脱节 | [12](./12-需求-AI-SEO与流量分析助手.md) §7 增补**发布管线**说明（走查 + 验收脚本） | 同上 |

---

## 2. 已知问题 / 待修复 / 待决策（登记状态）

以下**非**均已实现修复；关闭时在本表 §3 增加一行并改状态。

### 2.1 安全、部署与工程（与 [03](./03-开放事项总表.md) 一览一致）

| ID | 类型 | 说明 |
|----|------|------|
| **SEC-01** | 运维必做 | 生产须更换 **`JWT_SECRET`**、演示与管理员口令；代码仅拦截弱密钥，**不能替代**运维执行 |
| **OPS-ENV** | 部署 | **`ALLOWED_ORIGINS`**、**`VITE_*`** / **`PUBLIC_SITE_URL`**、HTTPS、DB 备份等须按环境验收 |
| **TC-EXEC** | 测试 | 发布前以 [09](./09-上线发布验收清单.md) **人工走查**为主 |
| **ENG-PG** | 工程 | 选用 **PostgreSQL** 时 **`DATABASE_URL`**、驱动、迁移与备份须**单独验收** |

### 2.2 AI SEO 与数据产品（见 [14](./14-AI-SEO与数据打通评估-项目现存问题.md) §6）

| ID | 简述 |
|----|------|
| **P-AI-03** | 竞品流量图为离线素材，未结构化入库（快照内已标注 **`offline_reference_only_not_in_db`**，结构化入库仍待办） |
| **P-AI-04** | **`page_seo` 抽样与 7 日/Top N 窗口**在大站可能偏差（快照内已标注 **`sorted_path_keys_first_50`** 策略，运营仍须与 Analytics 交叉核对） |
| **P-AI-05** | 异步 **202** 长任务仍为后续里程碑；同步调用存在网关/超时风险（快照 **`snapshot_limits_and_caveats`** 已提示） |
| **P-AI-06** | AI 限流为**进程内**，多 worker 不共享（快照已登记；生产多 worker 须 Redis 等分布式限流另立项） |
| **P-AI-07** | 出境合规、Markdown 渲染、成本等**产品决策**未闭合（快照已登记 **`open_product_decisions`**，闭合仍待产品/法务） |

### 2.3 控制面与商业化后续

- 见 [03](./03-开放事项总表.md) **§4.2 BACKLOG-CP**、[11](./11-控制面演进需求-CP-BACKLOG.md)、[10](./10-需求-商业化与订单.md)（续购、强曝光、法务话术等）。

---

## 3. 修复/状态变更记录（按日期）

| 日期 | ID / 主题 | 动作 |
|------|-----------|------|
| 2026-04-04 | 文档首版 | 建立本日志，归纳 §1 已落实项与 §2 登记项 |
| 2026-04-04 | P-AI-01/02、P-DOC-01/02 | 索引占位符重命名与兼容、出站埋点、快照验收脚本与 CI、需求 12 §7 发布说明；§2.2 收敛为仍开放项 |
| （待填） | （关闭某条时） | 在表格中标注 **已关闭** 与 PR/提交引用 |

---

## 4. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-04 | 首版：与 03/04/05/14 对齐，区分「已落实」与「仍开放」。 |
