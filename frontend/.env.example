# 生产环境：前台 canonical / OG 用的公网根（不要尾斜杠）。应与后端 sitemap 使用的 PUBLIC_SITE_URL 同域。
# VITE_PUBLIC_SITE_URL=https://www.example.com

# 生产环境：打包后浏览器请求 API 的根 URL（不要尾斜杠）。
# VITE_API_BASE=https://api.example.com

# 开发：浏览器请求始终相对当前访问地址的 /api（与运行地址一致）；勿在开发 .env 里填 localhost:8000 的 VITE_API_BASE。
# 仅当开发时要直连后端、不经 Vite 代理时，再设下面一项（无尾斜杠）：
# VITE_DEV_API_BASE=http://127.0.0.1:8000

# 仅开发：Vite 把 /api 转发到的后端根地址（本机后端监听）
DEV_API_PROXY=http://127.0.0.1:8000
