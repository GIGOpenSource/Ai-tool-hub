---
description: 自研源码每行须有注释；注释须为语法注释，运行期不参与逻辑、不被业务读取
alwaysApply: true
---

# 逐行注释与「零运行期说明」约定

## 适用范围

本仓库**自研源码**（如 `backend/app/`、`admin/app/`、`admin/components/`、`admin/lib/`、`frontend/src/`、`backend/scripts/`）。**不适用**第三方与生成物：`node_modules/`、`.next/`、`.venv/`、`dist/`、`build/` 等。

## 强制要求

1. **每一行产生行为的代码**（import、声明、赋值、调用、控制流、导出等）都须有**紧邻的中文或中英文说明**：
   - 同一行末尾注释，或
   - 紧贴其上的独立注释行（仅当一行过长或 JSX 不便行末注释时）。
2. **纯空白行**可以不写注释。
3. **成对的括号行**（如单独的 `}`、`)`）若上一行已说明块结束意图，可在闭合行用短注释（如 `// 结束 if`），或在与开括号对应的控制语句处已说清则可只标注关键闭合行——以「读者能对应结构」为准，避免无意义重复。

## 注释形态：必须「语法注释」，运行期不可见/不可执行

说明文字**只能**使用语言规定的注释语法，不得在运行时被读取或求值：

| 语言 | 允许 | 禁止（运行期会参与或产生副作用） |
|------|------|----------------------------------|
| Python | `# ...` | 用 `"""..."""` / `'...'` **单独成行**当「注释」（会构造字符串对象；模块/类 docstring 会进入 `__doc__`）。**仅给人看的说明请用 `#`**。 |
| TypeScript / JavaScript | `//`、`/* */` | 用模板字符串或普通字符串承载说明（会被打包、可能被摇树/常量折叠，但仍属运行期数据）。 |
| 其他 | 按语言标准注释 | 任何会被解析为表达式/字面量的「伪注释」 |

TypeScript/JavaScript 中 **`///` 三斜杠指令**（如引用路径）会参与工具链，仅在手写声明文件等需要时使用，**不要**滥用承载普通说明。

## 修改既有代码时

增删改任何逻辑行时，同步补齐/更新该行注释，保持「代码与注释一致」。请勿删除与当前任务无关的既有注释。

## 给 Agent 的检查清单

- [ ] 非空非纯括号行是否有注释？
- [ ] 说明是否全部在 `#` / `//` / `/* */` 内？
- [ ] Python 是否避免了用三引号字符串代替说明？
